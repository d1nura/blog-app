import { View, Text } from "react-native";
import { SignUpForm } from "./atoms/SignUpForm";

export const Account = () => {
    return (
        <View>
            <SignUpForm />
        </View>
    );
};
